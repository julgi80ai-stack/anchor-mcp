-- SPDX-License-Identifier: Apache-2.0
-- v6 → v7: 인용문이 생성 시점에 원문에서 몇 번 나왔는지 기록한다 (D-231).
--
-- 앵커는 첫 출현에 묶인다(D-047). 인용문이 문서에 여러 번 나오면 사용자가
-- 인용한 인스턴스가 삭제돼도 다른 인스턴스가 잡혀 `INTACT`가 된다. 그
-- 모호성은 `cite` 응답의 경고 문자열로만 존재했고 어디에도 남지 않아,
-- **다른 세션에서 재검증하는 사용자는 알 길이 없었다.**
--
-- 기존 행은 `NULL`이다 — 그때 몇 번 나왔는지는 저장된 적이 없으므로
-- 되짚어 셀 수 없고(그 사이 본문이 바뀌었을 수 있다), 1로 채우면 모르는
-- 것을 "유일하다"고 단정하는 것이 된다. 모를 때는 모른다고 적는다.
ALTER TABLE anchors ADD COLUMN occurrences INTEGER;
