# 이 프로젝트가 무엇인가 — 고치기 전에 읽는다

> **Anchor는 AI가 사용한 웹 근거를 시간에 걸쳐 다시 검증할 수 있게 한다.** (MANIFESTO §0)

우리는 **검색하지 않는다. 판단하지 않는다.** 이 두 부정으로 시작한 MCP다.
소비자는 사람이 아니라 **AI 에이전트**다. 우리가 하는 일은 에이전트에게 **사실을 정확히 건네는 것**이고, 그 사실로 무엇을 할지는 에이전트가 정한다.

## 정체성이 닳는 자리 (수천 줄의 디버깅이 실제로 남긴 흔적)

고치다 보면 "조금 더 똑똑하게" 굴고 싶어진다. 그때마다 우리는 조금씩 다른 물건이 됐다.

- **301을 "같은 리소스"로 믿고 인용의 정본 URL을 바꿨다** — 판단하지 않기로 해 놓고 URL의 **정체성을 판정**하고 있었다 (B-1)
- **문서의 1%만 보고도 `unchanged`를 다른 문서와 같은 확신으로 말했다** — 모르는 것을 모른다고 하지 않았다 (A-1)
- **죽은 호스트를 "robots.txt가 거부"라고 말했다** — 사실이 아닌 문장으로 원인을 사이트 소유자에게 뒤집어씌웠다 (D-228)
- **첫 페치 60건을 "changed 60"이라고 보고했다** — 도구가 자기 자신에 대해 거짓을 말했다 (D-227)
- **403을 세 번 더 두드리고 7초 뒤에 "403"이라고 답했다** — "403은 403으로 보고한다"고 적어 놓고 (D-237)

전부 코드 버그이기 이전에 **정체성의 이탈**이다. 결함은 고치면 사라지지만, 이탈은 다음 조치에서 같은 모양으로 돌아온다.

## 고치기 전에 묻는 세 가지

1. **이건 사실인가, 판단인가?** 판단이면 하지 않는다. 사실만 건네고 에이전트가 판단한다. `ALTERED`가 오탈자인지 입장 번복인지 해석하지 않기로 한 것과 **같은 규칙**이다 — 그 규칙은 URL의 정체성에도, 문서의 동일성에도 똑같이 적용된다.
2. **모르면 모른다고 말하는가?** 아는 척하는 것이 침묵보다 나쁘다. 커버리지·출처·모호성·실패 사유는 전부 "우리가 무엇을 **모르는가**"의 고지다. 에이전트는 그 고지를 받아야 스스로 확인할 수 있다.
3. **덜어낼 수 있는가?** 인프라의 신뢰는 능력이 아니라 **절제**에서 나온다(MANIFESTO 원칙 3). 이 캠페인에서 옳았던 조치는 거의 항상 기능을 더한 것이 아니라 **판단을 덜어낸 것**이었다.

## 절대 넘지 않는 선 (MANIFESTO §6)

- 검색·크롤링·LLM 호출·안티봇 우회·텔레메트리는 **구현하지 않는다**. **403은 403으로 보고한다.**
- **로컬 우선** — 서버에 물어보지 않는다. 근거를 검증하는 도구가 정작 자기는 검증 불가능한 원격 서비스라면 문제를 옮긴 것일 뿐이다.
- **사용자가 한 명이어도 이득** — 네트워크 효과에 의존하는 기능은 넣지 않는다.
- **보이지 않아도 된다** — 잘 되면 사람들이 이름을 몰라도 된다. 그것이 실패가 아니라 목적이다.

---

# AGENTS.md

Anchor — AI가 사용한 웹 근거의 시간적·출처적 무결성 계층. Memento(RFC 7089)의 로컬 클라이언트.
사양은 `docs/SPEC.md`, 판단 기준은 `docs/MANIFESTO.md` §6·§7, 설계 근거는 `docs/decisions/`.
문서마다 한국어판(제목 `(KR)`)과 영문 트윈(`*.en.md`, 제목 `(EN)`)이 있다. **한국어판이 정본**이다.

## 반드시 지킬 것

- **이 파일(AGENTS.md)은 로컬 전용이다**: git에 커밋하지 않는다 (.gitignore에 등록됨, 2026-08-17 사용자 결정). 커밋 이메일은 현재 주소를 유지한다.

- **범위**: 검색·크롤링·LLM 호출·안티봇 우회·텔레메트리를 절대 구현하지 않는다. 403은 403으로 보고한다 (SPEC §1.3, §5.4).
- **라이선스**: `trafilatura>=1.8.0` 하한은 기능이 아니라 라이선스 요구다(미만은 GPLv3+). 낮추지 말 것. 카피레프트(GPL/AGPL/LGPL) 의존성 금지. 새 의존성 추가 시 `THIRD-PARTY.md` 갱신.
- **스키마 변경은 한 라운드에 하나로 묶는다** (2026-08-20, 9단계 등재). 이 세션에 v6 → v11로 **다섯 번** 올렸다 — 라운드마다 열 하나씩 따로 붙인 결과다. 마이그레이션은 **되돌릴 수 없고**(R7), 하나 늘 때마다 v1부터의 전 경로 마이그레이션 테스트가 늘고, 표 재작성이 섞이면 그만큼 위험이 곱해진다. 한 라운드에서 열이 여럿 필요하면 **마이그레이션 파일 하나로** 낸다. 라운드가 끝난 뒤 "하나만 더"가 생기면 그것은 다음 라운드다.

- **ADR은 동결**: `docs/decisions/`를 고치지 않는다. 판단이 바뀌면 새 ADR로 대체. **영문 트윈도 동결**이며, 새 ADR을 쓰면 트윈도 함께 만든다.
- **용어**: reference rot / citation drift. "content drift"는 쓰지 않는다. "세계 최초"·"새로운 표준" 금지 (MANIFESTO §11).
- **판정 경계**: `ALTERED`의 의미(오탈자냐 입장 번복이냐)는 판정하지 않는다. 사실만 보고한다.
- **문서 정합**: 구현이 SPEC과 달라지면 코드만 고치고 끝내지 않는다. SPEC(필요 시 버전 개정 + 변경 이력 절)과 README 등 관련 문서를 같은 커밋 흐름에서 일치화한다 (2026-08-17 사용자 지시).
  **한국어판을 고치면 영문 트윈도 같이 고친다.** 절 번호는 두 판본이 동일해야 한다(상호참조가 양쪽에서 성립해야 하므로).

- **SPEC과 코드가 어긋날 때 어느 쪽을 고치나** (2026-08-18 사용자 지시): "무엇이 먼저 정해진 것인가"로 정한다.
  - **코드를 고친다** — SPEC이 외부 표준(RFC·W3C·MCP)을 옮겨 적은 것일 때 / 프로젝트 계약·정체성일 때(판단은 MANIFESTO §6·§7) / 구현 가능한데 아직 안 한 것일 때.
  - **SPEC을 고친다** — SPEC의 **사실 주장**이 틀렸을 때 / SPEC **내부가 모순**일 때 / SPEC이 **스스로 도입한 변경**을 못 따라왔을 때 / 그 기능을 **안 하기로 결정**했을 때(비목표로 이관).
  - **금지**: 구현이 어렵다는 이유로 SPEC을 낮추는 것. 그건 정합이 아니라 은폐다. 모든 SPEC 개정은 **왜 바꿨는가**를 변경 이력 절에 남긴다.
  - 판단이 애매하면 낮추지 말고 사용자에게 묻는다.

- **조치 절차** (2026-08-18 사용자 지시 — 2차 감사 101건이 치른 대가):
  1. **픽스처를 코드보다 먼저 만든다.** 결함이 사는 조건을 재현하는 픽스처가 먼저다. 순서를 바꾸면 고친 뒤에도 회귀를 잡지 못한다.
  2. **재현 스크립트 통과는 완료가 아니다.** 그건 결함의 한 발현일 뿐이다. 같은 결함이 **다른 진입점·다른 단계·다른 호출 경로**에 있는지 확인한 뒤에 완료로 적는다. (이 확인을 빠뜨려 9건이 되살아났다: D-042→D-109, D-044→D-115, D-034→D-118, D-009→D-125, D-037→D-054)
  3. **조치 코드는 새 코드이고, 따라서 새 결함원이다.** 조치가 끝나면 그 코드를 결함처럼 취급해 다시 감사한다. 1차 조치는 `src/`의 약 40%를 새로 썼고, 2차 감사 101건 중 3분의 2가 거기서 나왔다.
  4. **픽스처에 정상값을 하드코딩하지 않는다.** 실제로 이렇게 숨었다 — 행이 0인 마이그레이션 DB, `current_version`이 NULL인 gc 픽스처, `ttl=60000`, 리다이렉트 없는 동시성 테스트, `<pre>`·표·CJK가 0건인 골든 30건.
  5. **회귀 테스트가 늘어난 것과 새 코드가 감사된 것은 다른 일이다.** 테스트 수는 후자의 증거가 되지 못한다.
  6. **픽스처는 결함보다 축이 먼저다.** 결함을 이해한 뒤 그 발현 하나를 재현하면 같은 축의 이웃 결함이 전부 살아남는다 — 세 라운드 연속 같은 실패였다(3단계: 인용문이 전부 한글 90자라 길이 축 부재 → D-178 치명 잔존 / 4-가: robots가 항상 200이라 상태코드 축 부재 / 4-나: 어느 경로든 같은 HTML이라 본문 구분 축 부재). 새 영역을 조치할 때는 그 영역의 현실적 축(입력 길이·상태코드·문자 체계·경로별 응답·타이밍 비의존)을 픽스처에 **먼저 열고**, 그 위에서 결함을 재현한다.
  7. **검증 명령의 출력은 자르지 않고 전부 본다.** `tail`로 자른 벤치마크 출력에 근거해 "게이트 유지"를 적었다가 거짓 기록이 됐다(4-가). 게이트·되돌림 검증은 전체 출력을 본 것만 기록한다. 되돌림 검증은 첫 줄에서 `module.__file__`을 찍어 **사본을 임포트했음을 증명**한다(venv 편집가능 설치 함정, 3회 재발).

## 상태 (2026-08-20, **9단계 종결** · 조치 코드 재감사 진행 중)

**v1.15.0 — 2차 감사 캠페인 0~9단계 종결.** 9단계(코드 품질)를 이 세션에서 닫았다.

| | |
|---|---|
| 테스트 | **1266 통과 / 1 skip** (노드 1,267) |
| SPEC | **v1.18** (KR·EN 동기, 이력 절 18개, 절 §32까지) |
| 스키마 | **v10** (9단계는 스키마를 건드리지 않았다 — R7) |
| 패키지 | **1.15.0** (9단계에서 src 행동 무변경이므로 유지) |
| §10 게이트 | 9 → 14 → 17 → 19 → **22종** |
| 미푸시 | **20커밋** (사용자 결정: 푸시 보류) |
| CI | 태그·수동만 (`test`·`license`). `benchmark`는 주 1회 크론(ubuntu 2잡 ≈ 무료한도 2~4%) |

### 9단계 결과 — **계획 7개 중 셋의 전제가 틀렸다**

실측이 계획을 반박했다. 재보지 않고 손댔으면 이득 없는 대규모 diff와 그 검토 비용만 남았을 것이다.

| # | 항목 | 결과 |
|---|---|---|
| 1 | service.py 분리 | ✔ `calls/` 3모듈, 1,763 → 1,555줄 |
| 2 | 사실 공개 필드 19개 | **닫음** — 진짜 불일치는 1개, 그것도 의도 (사용자 결정) |
| 3 | 게이트 겹침 | **지울 것 없음** — 대신 눈먼 게이트 셋을 조치 |
| 4·7 | 대장 분할·스키마 규칙 | ✔ |
| 5 | 테스트 경계 | 경계가 **장식**(읽는 곳 0) — `anyio_backend` 9→1만 |
| 6 | 이력 주석 | **조치 없음** — 주석 12%, 60%가 포인터, 거짓 주석 0건 |
| — | verify 분할 (목록 밖) | ✔ 269 → **138줄**, 분기 38 → 20 (사용자 지시로 별도 트랙) |

**스파게티는 실측에서 확인되지 않았다**: 죽은 함수 0 · 중첩 4단 이상 3% · 본문 같은 테스트 0 · 코드에 없는 식별자를 부르는 주석 0. 확인된 실제 부채는 큰 파일 하나(갈랐다)와 뜨거운 함수 둘(하나 갈랐다).

### 이 세션의 큰 것 넷

1. **지키는 척하던 게이트 셋을 실제로 지키게 했다.** 정상 코퍼스 게이트는 사실상 `str.find` 감지기였다(문맥·퍼지·`_context_supports`를 각각 죽여도 셋 다 통과). **SPEC §10이 v1.7에서 이미 적어 두고 아홉 판본을 지나도록 조치되지 않은 자리다.**
2. **"실패는 빠르다" 게이트가 자기 계약을 어기고 있었다** — 재시도 상한 회귀 시 `sleep(3600)`으로 **빨간 줄이 아니라 응답 없는 CI 잡**이 됐다. 케이스별 마감 도입.
3. **전경 예산 집행 게이트**(v1.18) — 판정을 **벽시계가 아니라 상태**로 한다(구속 시 `UNRESOLVED`, 회귀 시 완주해 `MISSING`). 기계 속도와 무관해 얇은 여유 문제를 원리적으로 피한다.
4. **증명 도구 자신의 구멍을 메웠다** — R3 지문이 `_`로 시작하는 이름과 재수출을 못 봐서 "삭제 0"이 **안 봐서 나온 0**이었다. `dump_namespaces()` 추가.

### 이 세션이 남긴 규칙 (규약 §6~8)

- **되돌림의 축은 통과/실패만이 아니다.** 이름·개수·순서를 지키는 코드는 그 축에서 재라. 차이가 없다는 것은 "죽은 코드"의 증거가 아니다.
- **구역 한정 치환에서 `count >= 1`은 단언이 아니다.** 정확한 개수를 적고, 구역 밖 바이트 동일을 별도 증명한다. (이걸 어겨 288개가 깨졌고 R7로 되돌렸다.)
- **"옮겼다"는 역변환 해시 대조로 증명한다.** diff를 눈으로 보는 것보다 강하다.
- **정리 항목의 전제부터 재라.** 인상으로 적은 항목 7개 중 셋이 틀렸다.

**재감사 완료 (2026-08-20)**: 치명 0 · 높음 2(둘 다 조치, 커밋 09cb631) · 낮음 3 · 관찰 9.
- **E-1 (내가 만든 결함)**: 마감을 달며 넣은 `except Exception`을 **판정이 보지 않아**, 404 경로가 통째로 깨져도 게이트 22종 전부 PASS·종료코드 0이 나왔다. 조치 전에는 예외가 벤치를 죽여 CI가 빨갰다 — **마감이 도구를 덜 정직하게 만들었다.** 고치려던 병을 같은 자리에 새로 만든 것이다(조치 절차 3번의 교과서적 재현).
- **F-1 (선재)**: sdist가 `.hypothesis/` 434항목·홈 절대 경로 289건을 실었다. 653 → 219항목, 유출 0. **조치 중 `.gitignore` 주석에 그 경로를 예시로 적어 같은 유출을 한 번 재현했다** — 빌드해서 열어 보고 잡았다.

**감사가 남긴 정정 (내가 과장했던 것)**:
- `34/500`은 독립 표본이 아니다 — 4단계 도달 앵커는 **distinct 14개**이고 나머지는 패딩이다. SPEC과 커밋에 표본처럼 적었다.
- **드리프트 축은 3단계(문맥 매칭)에 대해 판별력이 정확히 0**이다. `_match_by_context`를 죽여도 211개 앵커의 상태·위치·편집거리가 한 건도 안 바뀐다. "판정 기제가 한 줄도 실행되지 않았다"를 고쳤다고 적었지만, 문맥 매칭은 실행만 되고 여전히 게이트가 안 지킨다.
- 211개 중 **133개는 지금도 1단계(`str.find`)에서 끝난다.** `_context_supports`는 이 코퍼스에서 **항상 True**만 돌려준다(False 분기 미실행). regex 퍼지 경로는 **0회 호출**.
- 마감 도입으로 `Anchor()` 생성이 계측 구간 **안으로** 들어와 실패 케이스가 **8ms → 73ms**(6~7배). 상한 500ms라 통과하지만 독스트링의 "전부 20ms 안"은 더 이상 맞지 않는다.
- 같은 되돌림 수치가 코드 `1,055ms` · SPEC `1,066.9ms` · 감사 실측 `1,024.5ms`로 셋 다 다르다. 셋 다 "실측"이라 적혀 있어 어느 실행인지 알 수 없다.
- 5표본의 `percentile(.99)`는 사실상 **최댓값**이다 — "p99"라는 이름이 실제와 다르다.

## 내일부터의 실사용 — **반드시 확인된 것**

- MCP 등록: `/home/morpheus/anchor-mcp/.venv/bin/anchor-mcp`, `env.PYTHONPATH=""` (`~/.Codex.json`).
- **임포트 경로가 작업 트리다**: `anchor.__file__ = /home/morpheus/anchor-mcp/src/anchor/__init__.py` (편집가능 설치). 즉 **소스를 고치면 다음 프로세스부터 반영된다.**
- 실사용 DB `~/.anchor/store.db`(2.3MB)가 지금 코드로 정상 개방. `anchor stats` 동작 확인.
- 버전 보고는 소스 상수(`__init__.py`)에서 온다 → `anchor --version`·MCP 둘 다 **1.15.0** 정확. 설치 메타데이터가 `1.1.0`으로 낡았지만 **아무도 읽지 않는다**(무해, 재설치로 건드리지 않았다).

> **함정 (2026-08-20 실측)**: 이미 떠 있는 서버 프로세스는 **옛 코드를 메모리에 들고 있다.** 이날 PID 2052(8/19 22:27)·924167(8/20 13:07)가 첫 커밋(14:06)보다 앞서 시작돼 있었다. 파이썬은 시작 시 모듈을 한 번 읽으므로 소스 수정이 반영되지 않는다. **코드를 고친 뒤에는 세션을 재시작해야 그 코드가 돈다.**

## 관찰 창 현황 (연출하지 않고 사실대로)

`anchor stats` (2026-08-20): 문서 106 · 버전 106 · 앵커 68 · 디스크 2.36MB.
최근 30일 요청 208 (cache_hit 28 · not_modified 31 · unchanged 15 · created 106 · error 28).
**hit_rate 28.37%** — v0.4 완료 기준 80%에 크게 못 미친다. 표본이 얇고(창 개시 8/17) 첫 페치가 106건이라 구조적으로 낮다. **기준 조정이 필요하면 SPEC 개정이나 새 ADR로 남길 것** — 수치를 연출하지 않는다.

**다음**: 배포 준비(PyPI·Zenodo·MCP 레지스트리). 미푸시 **21커밋**, 푸시는 사용자 결정.

## (이전) 상태 (2026-08-19, 5단계 종결 시점)

**v1.8.0 — 2차 병렬 감사 조치 진행 중, 0~5단계 종결.** 5단계는 본조치 8 + 감사 2라운드 14 + 자가 1 = 22건으로 닫았다(커밋 675324f·bccf84e·bc474e9). 남은 **미조치 32건**(아래 인수인계 절). 대장은 `docs/internal/AUDIT-LOG.md`(git 미추적, 2026-08-20 분할) — 라운드별 조치 기록·감사 기록·교훈·**5단계 종결 판정**이 전부 거기 있다. **로컬 main이 origin보다 18커밋 앞**(캠페인 전체 미푸시) — 푸시는 사용자 결정 대기.

- 테스트 198 → 340(1차) → 583(4단계) → **613**(5단계 + 감사 2라운드). §10 게이트 **9종** PASS — 부하 중 캐시 히트·전경 경합(경합 p99·UNRESOLVED 증분·전경 유효성)·부하 생존 게이트가 5단계에서 신설됐다(유휴 벤치만으론 D-120·D-196이 CI를 통과한다 — 게이트는 실제 배치에서 재야 한다). cache_hit는 순수 판정 함수 `judge_cache_hit`(바닥이 예산 절반을 넘으면 순비용 기준, D-209).
- 스키마 v5 → **v6**(`versions.last_observed_at`·`last_observed_seq` — 관측의 시간축, D-083). NORM_VERSION **3**.
- SPEC **v1.9 (KR·EN 동기 완료)**. §15(v1.9)·§16(v1.8)·§17(v1.7)이 이 감사 흐름의 변경 이력이다.
- 신규 공개 예외: `anchor.errors.InvalidURL`(입력 오류 — robots 거부로 둔갑 금지).
- 5단계 신규 공개 표면: `anchor.server.serve_forever`(두 진입점 공유 종료 경로), `anchor.anchoring.approx.set_thread_yields`/`foreground_section`(배경 워커 전용 GIL 양보 — 전경 호출 진행 중에만 잠들고, 잠든 시간은 `Budget.credit`으로 상환하되 예산의 15% 상한). 5단계는 본조치 8 + 감사 6 + 재감사 7 + 자가 1 = **22건 조치로 종결**(커밋 675324f·bccf84e·bc474e9). 재감사분(bc474e9)의 3차 감사는 의도적 생략 — **6단계 감사 범위에 bc474e9 잔여를 포함할 것**(대장 종결 판정 참조).

## 디버그 인수인계 (다음 세션은 여기서 시작)

**작업 방식은 위 "조치 절차" 1~7이 전부다.** 특히 6(축 먼저)·7(출력 전체·`__file__` 증명)은 이 세션에서 세 번씩 재발한 실패에서 나왔다 — 건너뛰면 같은 자리에서 미끄러진다.

**남은 미조치 32건, 권장 순서** (대장에서 `미조치`로 검색하면 전문이 나온다). ~~5단계 Tasks·서버 8건~~ — **2026-08-18 완료**(D-116~123, 대장 "5단계 조치 기록" 참조):

1. **6단계 — 회계·설정·CLI (27건, 먼저)**: D-130(높음 — 예외로 끝난 실패가 `fetch_log`에 없어 hit_rate 2.67배 과대 — **실사용 관찰 창 판정을 오염시키므로 관찰 판정 전에 반드시**), D-131(높음 — `bytes_saved_estimate`가 captured_at 최대 버전 기준, 실측 1220배 과대), D-132~154. 축: 예외 경로의 회계, 되돌림 문서의 절감 계산.
2. **소품 (5건)**: D-085(`latest~-1`이 조용히 반대쪽 끝), D-086(`latest~abc` 생 ValueError), D-092(CDX statuscode 재확인 없음), D-093(cite/verify 응답에 `source` 없음 — SPEC §5.2 "항상 알 수 있다" 위반), D-097(robots_allowed 기록 오류 3종). 5단계 관찰에서 추가: `diff_versions(context_lines=음수)` 미검증.

**비용 판단 기준 (2026-08-18~19 사용자 지시)**: 미리 건너뛸 후보를 정하지 않는다 — **기본값은 전부 조치**다. 다만 조치 비용(복잡도·무게·속도 손실)이 결함의 해보다 명백히 커지는 **선택의 순간이 오면** 그때 판단한다. 판단 기준: ①"안 쓴 것보다 나빠지지 않는" 결함(캐시 미스 한 번 더, 성능 손해)은 비용이 크면 보류할 수 있다 — 반드시 비용·사유와 함께 대장에 기록하고, 애매하면 사용자에게 묻는다. ②"거짓을 말하는" 결함(회계 과대, 프로토콜 파괴, source 누락 같은 SPEC 위반)은 존재 이유(근거의 무결성)를 깎으므로 비용과 무관하게 잡는다. D-130·131은 ②이고, 특히 v0.4 관찰 창의 측정 도구 자체를 오염시킨다.

**보류·관찰 (조치 아님, 대장에 사유 있음)**: D-070(기각), D-075(별건 — 2단 PDF, 정규화 계층에서 못 품), D-160(백슬래시 해제는 멱등성을 깨서 보류), D-162(폴백 노이즈 — D-073과 함께 별도 설계), D-167 잔여(표·코드 울타리 유연 검색), D-173(trafilatura가 ZWJ 소실 — 의존성 내부, 테스트로 고정해 둠). 4단계 감사의 미조치 관찰 2건: 아카이브 호스트 자신의 robots 5xx 시 구제가 조용히 사라지는 문제(SPEC 미규정), URI-M robots 요청의 bytes_down 미합산·레이트 제한 미적용. 5단계 관찰 2건: 동기(비-task) verify는 GIL 양보를 켜지 않음(그 응답을 기다리는 클라이언트에게 세금 부당 — 동시 다중 클라이언트 겹침 시 짧은 점유 가능), fetch 재시도 백오프가 취소 신호를 모름(취소 반응 상한 = 진행 중 요청 1건의 타임아웃+백오프, 유한이라 계약은 성립).

**이 머신의 함정 (재발 3회 이상)**:
- 되돌림 검증·사본 실험 시 venv의 편집가능 설치가 원본을 임포트한다 → `PYTHONPATH=<사본>/src:<사본>` + 첫 줄 `__file__` 출력 필수.
- 테스트는 반드시 `env -u PYTHONPATH .venv/bin/python -m pytest`(ROS 오염).
- 이 머신의 SQLite 커밋 p95는 부하에 따라 1.5~22ms를 오간다 — cache_hit 게이트가 흔들리면 코드 회귀가 아니라 커밋 바닥부터 확인(벤치가 스스로 출력한다).
- 전체 스위트 직후의 벤치마크는 수치가 부풀 수 있다 — 판정은 단독 실행으로.
- `pgrep`/`pkill -f`의 패턴이 **명령 자신의 셸 커맨드라인과 일치해 자기를 죽인다**(exit 144, 세션 내 2회 재발). 잔존 확인은 `pgrep -af "[p]attern"`(자기 제외 브래킷), 정리와 실행은 **별도 명령**으로.
- GIL 양보 수단은 이 머신에서 실측 필수: `time.sleep(0)` 84µs/호출(그러나 유일하게 유효), `os.sched_yield` GIL 미해제, `select(0)` 초고속 재획득이 대기자를 굶김. 근거는 대장 5단계 기록.

**단계 종료 의식(빠뜨리지 말 것)**: 조치 → 되돌림 검증(전 기제 판별력) → 전체 스위트+벤치(전체 출력) → 대장 기록 → SPEC KR·EN **동시** 개정 → README·버전 → 커밋(본문에 D번호별 증상→조치, `tests: +N — 총 N 통과` + Co-Authored-By) → **그 단계 조치 코드를 감사 에이전트로 재감사**(Opus 1기, "없으면 없다고 보고" 명시, 수리 금지·보고만). 감사 프롬프트 형식은 대장의 3·4단계 감사 기록이 견본이다.

## 차기 과업 0 — **9단계: 코드 품질 (지금 가장 큰 부채)** (2026-08-20 사용자 지정)

> **사용자의 진단**: *"지금 코드는 말 그대로 10명의 개발자가 오케스트레이션 없이 서로의 브랜치를 공유나 조율 없이 밀어넣은 듯한 형태다. 스파게티 코드에 가까워지고 있다. 아마추어식 개발을 한 것은 아닌데, 디버그나 기술부채를 단기간에 해결하며 장기적으로 유지보수 비용이 막대하게 커지는 식의 코딩을 했다. 어쩔 수 없지. 코드 품질 개선이 현재 우리가 직면한 가장 큰 부채이다."*

**이 진단은 옳다.** 실측(2026-08-20):

```
src/       8,994줄 / 29파일
  service.py     1,763줄 · 메서드 43개 · 서로 무관한 클래스 4개가 한 파일에
                 (_Traffic 회계 · _UrlLocks 동시성 · _CallGate 수명 · Anchor 파사드
                  — 각각 다른 라운드가 밀어 넣었다)
  repository.py  1,588줄
D번호 코드 내 언급 504회 · 고유 216개   ← 고고학적 지층
tests/    16,069줄 / 64파일 (소스의 1.8배)
models.py 필드 99개 중 19개가 같은 종류("사실 공개")
스키마    한 세션에 v6 → v11 (5회 상승)
§10 게이트 9 → 17종
```

**증상은 아마추어 코드의 것이 아니라 "빠른 조치가 누적된 코드"의 것이다**: 응집도 낮은 거대 모듈, 같은 개념이 여러 이름으로 흩어짐, 이력 주석이 코드를 덮음(service.py 15%), 테스트가 소스보다 큰데 파일 경계가 관심사와 무관.

### 이 단계의 성격 — **오직 덜어내기**

새 기능도, 새 결함 사냥도 하지 않는다. 정체성 블록의 세 번째 물음("덜어낼 수 있는가")을 **판단이 아니라 코드 모양에** 처음으로 적용한다.

### 규약 문서 — **착수 전에 반드시 읽는다**

**`docs/internal/REFACTOR-PROTOCOL.md`** (git 미추적)가 9단계의 정본 절차다. 하드 규칙 7개(R1~R7)와 매 단계 절차가 거기 있다. 에이전트에 맡길 때는 **그 문서 전문을 브리핑에 싣는다.**

요지만 옮기면:
- **R1** 테스트를 고치지 않고 초록이어야 한다 (임포트 경로만 예외)
- **R2** **테스트 개수가 줄면 안 된다** — 파일을 옮기다 임포트를 놓치면 pytest가 수집을 못 해 **초록인 채로 조용히 줄어든다**
- **R3** `tools/surface_snapshot.py`의 지문이 빈 diff여야 한다 (임포트 경로·시그니처·dataclass 필드·MCP 도구·스키마)
- **R4** 새 코드를 쓰지 않는다 — `if`를 쓰고 있으면 그건 조치다
- **R5** 한 커밋에 한 관심사 (이분 탐색 가능해야)
- **R6** 결함을 발견하면 **등재만** 한다
- **R7** 되돌릴 수 없는 것(스키마·데이터 삭제)은 하지 않는다. 초록으로 못 만드는 단계는 **패치하지 말고 revert**

### 안전 도구 — 이것이 9단계를 가능하게 한다

**행동을 바꾸지 않는다는 것을 증명할 수단이 이미 있다.** 테스트 1,266건 + 게이트 17종은 이 캠페인이 만든 자산이고, 정리 단계의 안전망이 되라고 만든 것이나 다름없다.

> **하드 규칙: 모든 정리 단계는 테스트를 고치지 않고 초록이어야 한다.**
> 테스트를 고쳐야 한다면 **행동이 바뀐 것**이고, 그건 정리가 아니라 조치다 → 범위 밖.
> (예외: 파일을 옮기며 임포트 경로만 바뀌는 것은 행동 변경이 아니다.)

매 단계마다: `env -u PYTHONPATH .venv/bin/python -m pytest -q` 전량 + `benchmarks/run_micro.py` 단독 실행(전체 출력).

### 할 일

1. ~~**`service.py` 1,763줄을 관심사로 가른다.**~~ — **2026-08-20 완료** (커밋 a69790a). `anchor/calls/`로 갈랐다: `traffic.py`(23줄 `_Traffic`)·`locks.py`(80줄 `_UrlLocks`)·`lifecycle.py`(133줄 `_CallGate`·`_foreground`). 기존 서브패키지가 전부 도메인 이름이라, 도메인이 아니라 **한 번의 공개 호출에 붙는 기제**라는 뜻으로 `calls`를 골랐다. service.py **1,763 → 1,555줄**, 삽입은 import 5줄뿐이고 옮긴 네 블록은 원문과 **바이트 동일**. `_foreground`의 결합(GIL 양보 + 호출 계수)은 D-196·D-129가 각각 이유를 갖고 얹은 것이라 **풀지 않았다**(푸는 순간 행동 변경). 밑줄 이름도 그대로 뒀다 — 공개로 승격하면 지문에 새 공개 클래스가 찍혀 R3 위반이다. 증명: R1 테스트 무변경 · R2 1266 passed/1 skipped · R3 지문 삭제 0·변경 0·추가는 새 모듈 4줄 · 게이트 17종 PASS.
2. ~~**사실 공개 필드 19개를 공통 모양으로.**~~ — **2026-08-20 닫음(사용자 결정). 전제가 과장이었다.** 6개 클래스에 흩어진 20개 중 **진짜 불일치는 사실상 1개**다. 나머지는 이름만 닮은 서로 다른 사실이다(`source`=바이트 출처 · `coverage`=포착률 · `occurrences`=중복 출현 · `raw_changed`=시야 밖 변경 · `redirect`=정체성). `VerifyReport`의 `sources`·`ambiguous`·`low_coverage`는 **집계값**이라 낱개와 모양이 다른 것이 당연하다. 남은 후보 `AttentionItem.coverage_ratio`(스칼라) 대 `Coverage`(객체)도, AttentionItem은 앵커마다 생기고 Coverage는 판본에 속하므로 **응답 크기를 아낀 의도**로 읽힌다. 5번의 `_config` ×9와 같은 함정 — 추상 수준을 올리면 무엇이든 "한 가지 일"로 보인다. **와이어 포맷을 깨지 않고 종료.**
3. ~~**벤치 게이트 17종 중 겹치는 것.**~~ — **2026-08-20 완료, 전제가 틀렸다** (커밋 d4e7549, SPEC v1.17). 감사 1기가 17종을 되돌림으로 전수 확인한 결과 **완전히 겹치는 쌍은 없다** — 지울 것이 없었다. 대신 **둘이 눈이 멀어 있었다**: ①정상 코퍼스 게이트가 사실상 `str.find` 하나만 지킴(인용문을 대조 대상과 같은 텍스트에서 잘라 만들어 1단계가 항상 적중 — 문맥·퍼지·`_context_supports`를 각각 죽여도 셋 다 통과). **SPEC §10이 v1.7에서 이미 적어 두고 아홉 판본을 지나도록 조치 안 된 자리다.** 드리프트 축(공백만 다른 판본)을 열고 `MISSING` 0건을 독립 게이트로 세웠다 — 퍼지가 죽으면 인용문이 멀쩡한데 사라졌다고 보고하는데 그걸 잡는 게이트가 하나도 없었다. ②"실패는 빠르다" 게이트가 케이스별 마감이 없어, 재시도 상한 회귀 시 **빨간 줄이 아니라 응답 없는 CI 잡**이 됐다(sleep 3600). 마감 도입 후 10초 만에 FAIL을 찍고 나머지 케이스를 계속 잰다. ③최악 사례 게이트가 예산 집행을 안 지킨다는 **사거리를 공시**(§5.5와 같은 판단 — 범위를 넓히지 않고 침묵을 고친다). 게이트 **17 → 19종**.
4. ~~**대장 1,486줄을 셋으로.**~~ — **2026-08-20 완료.** `docs/internal/`에 `DECISIONS.md`(153줄)·`AUDIT-LOG.md`(1,340줄)·`LESSONS.md`(138줄)로 갈랐고 셋 다 git 미추적이다. `docs/DEBUG-LEDGER.md`는 **포인터**로 남겼다(어느 문서를 언제 읽는지 안내). 유실 0을 **줄 수 검산**(1,573 → 1,573)과 표본 대조로 확인했으며, 분할 시점 원본은 스크래치패드에 백업했다.
5. ~~**테스트 64파일의 경계 정리.**~~ — **2026-08-20 완료, 단 전제가 틀렸다** (커밋 572ab6c). unit/integration 경계를 **읽는 곳이 0곳**이다(`testpaths=["tests"]` 뿐, 마커·CI 선택 없음) — 장식이라 파일 이동은 순수 소음이다. **옮기지 않았다.** 대신 AST 전수 측정: 테스트 함수 677개, 본문이 같은 쌍 **0건**, assert 없는 테스트 **0건**, 이름이 겹치는 헬퍼 15종 중 **14종은 본문이 달라**(합치면 행동 변경) 손대지 않았다. 글자 그대로 동일한 것은 `anyio_backend` 9사본뿐 → conftest 1곳(-45 +8줄). 증명: **노드 ID 전수 1,267개 차이 0**(수가 아니라 이름까지 비교) · 1266 passed/1 skipped · src 변경 0바이트. 되돌림이 가르쳐 준 것과 새 규칙은 규약 §6에 있다.
6. ~~**이력 주석의 밀도.**~~ — **2026-08-20 측정 결과 조치 없음.** 전제("주석이 코드를 덮는다")가 틀렸다. src 주석 비율 **12%**(1,124/9,030), 주석 블록 414개 중 1~2줄 포인터형이 **60%**, 7줄 이상 서술형은 **6%**뿐이고 그 표본은 전부 "왜 뻔한 대안을 안 썼는가"라 대장으로 옮기면 **잃는다**. D번호 514회는 서로 다른 217개(평균 2.4회) — 같은 이유의 반복이 아니라 한 조치가 닿은 여러 지점이다. 대신 **거짓말하는 주석**을 쟀다: 코드에 없는 식별자를 부르는 주석 **0건**, 없는 경로 0건, 인용한 SPEC 절 27개 중 실재하지 않는 것 **0건**. 주석은 부채가 아니다.
7. ~~**앞으로 스키마 변경은 묶어서 한 번에.**~~ — **2026-08-20 등재 완료** (아래 "반드시 지킬 것"에 규칙으로 들어갔다).

### 금지

- 이 단계에서 **새 기능·새 판정·새 결함 조치를 하지 않는다.**
- 정리 중 결함을 발견하면 **대장에 등재만 하고 다음 단계로 넘긴다.** 이 규칙이 없으면 정리가 또 조치로 번진다 — 그것이 지난 여덟 단계에서 일어난 일이다.

### 종료 조건

위 일곱이 끝나면 멈춘다. 그리고 **배포한다.**

> **멈추는 기준 (2026-08-20 합의)**: 감사가 0을 반환하는 날은 오지 않는다 — 그것은 종료 조건이 될 수 없다. 기준은 **"감사 결과가 사용자 경험을 바꾸지 않는 수준"** 이다. 그리고 지금 결함을 가장 잘 찾아 줄 도구는 감사가 아니라 **사용자**다 — 30일 관찰 창이 실사용 거의 없이 돌고 있다(진공에서 다듬는 중).

### 결함 수렴 근거 (문이 제곱으로 열리는 게 아니다)

조치 대비 감사 산출 비율: 4단계 0.76 → 6단계 0.26 → 8단계 17/24 ≈ 0.71(단, 그중 6건은 문서 정합이고 코드는 11/24 ≈ 0.46). **기하급수적 감쇠이지 제곱이 아니다.** 제곱으로 느껴지는 이유는 **닫힌 문은 안 보이기 때문**이다. 남은 큰 질문은 "실사용에서 무엇이 깨지는가" 하나이고 그건 감사로 못 찾는다.

---

## 차기 과업 (공개 전환 준비 — 별도 계획 문서 만들지 말 것, 이 절이 유일한 기록)

공개 전환은 **사용자가 지시할 때** 진행한다. 여기 적어 두는 이유는 최종 국면에서 미끄러지지 않기 위함이지 바로 공개하겠다는 뜻이 아니다.

1. **PyPI 배포** — `anchor-mcp` 이름 확보 확인됨(2026-08-16). 배포 전 체크: `tools/audit_licenses.py --all-installed --fail-on-copyleft --check-floors`, sdist/wheel에 LICENSE·NOTICE·THIRD-PARTY.md 포함 확인. 다운로드 통계가 공개 실적 자산이 된다.
2. **Zenodo DOI 연동** — 저장소 공개 후 연동 → 릴리스마다 DOI 자동 발급. `CITATION.cff`에 DOI 필드 반영. 학술 인용 실적 파이프라인.
3. **MCP 레지스트리 등재** — 공식 MCP Registry 및 주요 디렉토리. 등재 문구는 MANIFESTO §11 메시지 자산·금지 표현 준수.
4. ~~**docs 영문 트윈**~~ — **2026-08-18 완료**. `(KR)`/`(EN)` 제목 규칙과 `*.en.md` 파일 규칙이 확립됐다. 이후 문서를 고칠 때는 두 판본을 함께 고칠 것(위 "문서 정합" 규칙 참조).

**남은 관찰 항목 (릴리스 후, 코드 작업 아님)**:
1. 30일 실사용 hit_rate 80% — 관찰 창 2026-08-17 개시, `anchor stats`로 판정.
2. ~~실웹 아카이브 구제 사례~~ — **2026-08-18 확보 완료**. 대상은 `https://timetravel.mementoweb.org/about/`(2025년 폐쇄된 LANL Time Travel — 이 프로젝트의 존재 이유가 된 그 서비스). 호스트 소멸 → 아카이브에서 복원(`outcome=archive`, Memento-Datetime 2025-09-01) → 인용 → 재검증 `INTACT`. 이 과정에서 D-053(호스트 소멸 시 폴백 미도달)을 발견해 함께 고쳤다.
3. **CI 상태 (2026-08-19 정정)**: `license-audit` 녹색. **`test` 매트릭스는 실패 중** — Windows 3종 각 46건(`os.geteuid` 등 플랫폼 의존), macOS 간헐 1~2건(벽시계 상한 단언). Ubuntu 3종은 통과. 이전 기록의 "3종 전부 녹색 확인 완료"는 **거짓이었다**(8/17·8/18·8/19 푸시 전부 실패 상태였다) — 7단계에서 조치 중. 벤치마크 anchoring 잡은 러너 IP 차단 시 "판정 불가" 경고 후 성공 종료가 정상.
   **앞으로 CI 상태는 실행 id와 잡별 결론을 함께 적는다** — "확인 완료"라는 요약은 스스로 거짓이 될 수 있다.

**실사용 관찰 창 (v0.4 완료 기준용)**: 2026-08-17부터 `anchor-mcp`가 이 머신의 Codex CLI에 사용자 범위로 등록됨(`Codex mcp add -s user anchor`, PYTHONPATH 무력화 포함). 사용자는 Codex Desktop을 쓰지 않고 CLI만 쓴다. 세션에서 웹 문서를 참조할 때 anchor 도구를 우선 사용하면 hit_rate 데이터가 쌓인다. 판정은 `cache_stats`의 last_30d. 실사용이 얇으면 지표를 연출하지 말고 사실대로 기록 후 기준 조정을 SPEC 개정/새 ADR로 남길 것.

**환경 주의**: 시스템 Python 3.10 → `.venv`는 `uv venv --python 3.12`로 생성됨. 셸의 `PYTHONPATH`에 ROS(humble) 경로가 있어 pytest가 오염된다 — **테스트는 `env -u PYTHONPATH .venv/bin/python -m pytest`로 실행할 것**.
